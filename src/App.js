import React from "react";
import RouterProvider from "routes/routerProvider";

const App = () => {
  return (
    <React.Fragment>
      <RouterProvider />
    </React.Fragment>
  );
};

export default App;
